<!DOCTYPE html>
<html>	
	<body>
		<h1>Calculator</h1>
		<form action='' method='post'>
			<div>
			  <label for='first-value'>First Value:</label>
			  <input id='first-value' name='first-value' type='text'/>
			  <label for='second-value'>Second Value:</label>
			  <input id='second-value' name='second-value' type='text'/>
			</div>
			<div class='operators'>
			  <legend>Operator:</legend>
			  <input id='add'
					 name='operator'
					 type='radio'
					 value='add' 
					 checked='checked'/>
			  <label for='add' class='operators'>Add (+)</label></br>
			  <input id='subtract'
					 name='operator'
					 type='radio'
					 value='subtract' />
			  <label for='subtract' class='operators'>Subtract (-)</label></br>
			  <input id='multiply'
					 name='operator'
					 type='radio'
					 value='multiply' />
			  <label for='multiply' class='operators'>Multiply (*)</label></br>
			  <input id='divide'
					 name='operator'
					 type='radio'
					 value='divide'/>
			  <label for='divide' class='operators'>Divide (/)</label>
			</div></br>
			<div>
				<button>Submit</button>
			</div>
		</form>
		@{
			if(IsPost){
				int first-value = Convert.ToInt32(Request["first-value"]);
				int second-value = Convert.ToInt32(Request["second-value"]);
				string operation = Request["operator"];
				int answer = 0;
				
				switch(operation)
				{
					case "add":
						answer = first-value + second-value;
						break;
					case "subtract":
						answer = first-value - second-value;
						break;
					case "multiply":
						answer = first-value * second-value;
						break;
					case "divide":
						answer = first-value / second-value;
						break;
				}
				
				<p>Answer: @answer </p>
			}
		}
	</body>
</html>